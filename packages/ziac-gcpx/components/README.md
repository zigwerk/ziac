# Component Manifests

Canonical registry manifests are published with `ziac-templates` during the
monorepo foundation phase. The source implementations in `src/` and those
registry records are verified together before release. This directory becomes
the manifest root when `ziac-gcpx` moves to its own repository.
