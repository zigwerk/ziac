# Ziac Product Landing Implementation Plan

1. Add failing deterministic tests for topology data and the visible landing-page contract.
2. Scaffold `apps/ziac-site` as an independent Solid/Vite workspace and add root Bun commands.
3. Implement deterministic topology data and the animated Three.js hero scene.
4. Implement the responsive landing page, scroll motion, mobile navigation, and private-beta dialog.
5. Add the real Ziac operations capture as a deployable product asset.
6. Run app tests, strict typecheck, production build, and the relevant root checks.
7. Start the local site and verify desktop/mobile rendering, interactions, console state, animation, reduced motion, and nonblank canvas in the in-app browser.
8. Compare the browser capture with the selected design target, fix P0-P2 drift, and save a passing `design-qa.md`.

## Agent-Native Positioning Revision

1. Add failing UI contracts for Zig-speed, specialist-agent, causal-graph, and global Cloud Run positioning.
2. Strengthen the hero thesis without losing existing-estate discovery as the entry point.
3. Add an accessible, animated agent scenario surface with realistic GCP, IAM, pricing, rollout, and Cockroach skills.
4. Reframe the operations proof around causal evidence and fast agent execution.
5. Re-run responsive browser QA, interactions, console checks, focused tests, production build, and repository checks.

## Pulumi And Terraform Differentiation Revision

1. Ground comparative language in current official Pulumi, Terraform, and Google Cloud documentation.
2. Add failing copy contracts for the comptime application boundary, GCP depth, observed-estate ownership, global Cloud Run abstraction, and causal agent runtime.
3. Add a responsive comparison surface that credits each incumbent's strengths while making Ziac's narrower advantage explicit.
4. Add a real `ZigService` comptime example and actual Ziac diagnostic rather than an invented code sample.
5. Re-run source-level contracts, strict typecheck, production build, browser QA, and the repository gate.

## Google Cloud First Positioning Revision

1. Add a failing UI contract for explicit Google Cloud-first, non-adapter messaging and GCP-native infrastructure diagrams.
2. Strengthen the hero eyebrow so GCP priority is a first-viewport signal.
3. Add a compact statement within the differentiation section covering Google API semantics, native topology visualisation, and global Cloud Run primitives.
4. Verify the new band at desktop and mobile widths, then re-run all site and repository gates.

## SolidStart And SEO Revision

1. Add failing contracts for the SolidStart route shell, prerendering, route-owned metadata, structured data, robots policy, and sitemap.
2. Replace the Vite SPA entrypoint with SolidStart app, client, server, and file-route entrypoints while preserving the existing product component and styles.
3. Configure `/` prerendering and stable production output using the latest compatible SolidStart, router, and metadata packages.
4. Add canonical, Open Graph, Twitter, robots, and software-application metadata to the route.
5. Verify the built HTML contains essential marketing copy and metadata before hydration, then run typecheck, focused tests, browser interaction checks, and the repository gate.

## How It Works Route

1. Add failing route contracts for scaffolding, bundled skills, Codex/Claude Code/Gemini harness choice, dashboard control, global provisioning, combined estates, third-party groups, and the agent-first operating thesis.
2. Add `/how-it-works` to SolidStart prerendering, route metadata, structured data, sitemap discovery, and primary navigation.
3. Implement a responsive narrative using the existing visual system, live Three.js topology, real Operations capture, CLI surfaces, harness tabs, and the governed control loop.
4. Label the not-yet-released `ziac init` command as a private-beta flow while grounding later commands in the current Ziac CLI and MCP kernel.
5. Verify route SSR, interactions, responsive layout, crawler metadata, production prerendering, focused contracts, and the repository gate.

## Why Zig And ZigEffect Routes

1. Add failing contracts for the shared multi-page navigation, `/why-zig`, `/why-zigeffect`, route metadata, prerender configuration, and sitemap discovery.
2. Extract the existing header and footer into shared Solid components without changing the homepage conversion dialog or How It Works interactions.
3. Implement `/why-zig` around explicit allocation, inspectable control flow, `comptime` application-to-infrastructure validation, cross-compilation, immutable containers, and global Cloud Run economics without benchmark or memory-safety overclaims.
4. Implement `/why-zigeffect` around typed effects, layers and scoped resources, causal evidence, statecharts and workflows, deterministic fault testing, provider-neutral agent handoff, and disclosed maturity/evidence limitations.
5. Reuse the live Three.js topology and real Operations capture as first-viewport product proof; extend the existing responsive visual system without nested-card or decorative-asset sprawl.
6. Run route contracts, strict site typecheck, production prerendering, metadata inspection, desktop/mobile browser QA, navigation interactions, console checks, and the repository gate.

## GCP And Zig Brand Fusion

1. Capture the current white/cobalt homepage at the source viewport and add failing contracts for semantic GCP, Zig, runtime, and risk colour tokens.
2. Add an unframed first-viewport GCP graph to Ziac comptime to Zig service bridge and split hero emphasis between cloud cobalt and compiled amber.
3. Extend the live topology model so global compiled entry and output wiring use amber without recolouring third-party resources.
4. Apply the semantic palette to agent roles, GCP-first proof rows, comptime evidence, workflow stages, and the final compatibility band while keeping interaction controls predominantly cobalt.
5. Compare desktop and mobile captures against the baseline, check text contrast and canvas readability, then run topology contracts, site contracts, typecheck, production prerendering, console checks, and the repository gate.

## Causal Graph Explainer

1. Add failing contracts for the homepage `causal proof` link, `/causal-graph` content, independent metadata, prerender configuration, and sitemap discovery.
2. Implement the dedicated Solid route around two complementary graphs, one end-to-end Cloud Run deployment lineage, agent graph queries, a proof contract, and explicit evidence limits.
3. Reuse the real Operations capture and shared marketing chrome; add responsive editorial graph structures that preserve the existing semantic GCP/Zig palette without introducing decorative assets.
4. Link the ZigEffect causal-runtime section into the explainer so visitors can move between runtime internals and the cross-system Ziac story.
5. Run focused tests, strict typecheck, production prerendering, metadata inspection, homepage-link interaction, desktop/mobile browser QA, console checks, and the repository gate.

## Agent Compile Feedback Loop

1. Add a failing homepage contract for the edit, compile, diagnose, repair, and affected-verification loop.
2. Reframe the comptime example around fast agent feedback before preview or provider RPC while retaining the concrete `ZIAC100` failure classes.
3. Add a compact semantic loop and a direct causal-proof link using the existing cobalt, amber, and mint visual language.
4. Run site tests, typecheck, production build, and focused desktop/mobile browser QA.

## Canvas, Brand, And Final Conversion Polish

1. Add failing contracts for a live homepage canvas, animated Three.js Ziac mark, official attributed Zig mark, and the redesigned private-beta close.
2. Replace the static Operations capture with the reusable topology scene inside a compact architecture-canvas tool surface.
3. Implement the reduced-motion-aware Ziac resource cube and use it in shared header/footer branding.
4. Add the official Zig Project mark and attribution, then rebuild compatibility and beta sections around specific product promises rather than a generic card.
5. Run site tests, typecheck, production build, desktop/mobile visual QA, canvas pixel checks, animation/reduced-motion checks, navigation checks, and console inspection.
